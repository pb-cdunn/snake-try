#import json, os, re

# comment b
rule b:
	input:
		bar="a/bar.txt"
	output:
		baz="b/baz.txt"
	shell:
		"cp {input.bar} {output.baz}"
# comment a
rule a:
	input:
		foo="data/foo.txt"
	output:
		bar="a/bar.txt"
	shell:
		"cp {input.foo} {output.bar}"
# comment footer
